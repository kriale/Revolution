
CREATE TABLE users (
    login text,
    password text,
    money double,
    items list,
    PRIMARY KEY (login)
 );

CREATE TABLE chat (
  id int, - ?
  users set,
  messages list,
  PRIMARY KEY (id)
);

CREATE TABLE ? (

  PRIMARY KEY()
);