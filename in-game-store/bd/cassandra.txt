CREATE TABLE users (
    login text,
    password text,
    money decimal,
    items list,
    PRIMARY KEY (login)
 );


CREATE TABLE items (
   id text,
   name text,
   price decimal,
   quantity int,
   PRIMARY KEY (id)
);

CREATE TABLE basket (
   id int,
   items list,
   PRIMARY KEY (id)
);

CREATE TABLE section (
  name text,
  PRIMARY KEY (name)

);