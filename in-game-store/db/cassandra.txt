CREATE TABLE users (
    login text,
    password text,
    money decimal,
    items list,
    PRIMARY KEY (login)
 );


CREATE TABLE items (
   id text,
   name text,
   price decimal,
   quantity int,
   PRIMARY KEY (id)
);

CREATE TABLE basket (
   id int,
   items list,
   userLogin text
   PRIMARY KEY (id)
);
